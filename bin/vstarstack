#!/usr/bin/env python3

import sys

import vstarstack.process
import vstarstack.cfg

program_project = vstarstack.cfg.get_project()
program_argv = [item for item in sys.argv[1:] if item[:2] != "--"]

vstarstack.process.run(program_project, program_argv, 'vstarstack')
